package orangehrmPageObjects;

import commons.BasePage;
import org.openqa.selenium.WebDriver;

public class DependentsObjects extends BasePage {
    WebDriver driver;
    public DependentsObjects(WebDriver driver) {
        this.driver = driver;
    }
}
