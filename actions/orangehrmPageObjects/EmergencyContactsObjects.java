package orangehrmPageObjects;

import commons.BasePage;
import org.openqa.selenium.WebDriver;

public class EmergencyContactsObjects extends BasePage {
    WebDriver driver;
    public EmergencyContactsObjects(WebDriver driver) {
        this.driver = driver;
    }
}
