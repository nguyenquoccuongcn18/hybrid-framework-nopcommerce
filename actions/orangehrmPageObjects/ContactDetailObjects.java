package orangehrmPageObjects;

import commons.BasePage;
import org.openqa.selenium.WebDriver;

public class ContactDetailObjects extends BasePage {
    WebDriver driver;
    public ContactDetailObjects(WebDriver driver) {
        this.driver = driver;
    }
}
