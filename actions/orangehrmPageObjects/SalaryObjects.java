package orangehrmPageObjects;

import commons.BasePage;
import org.openqa.selenium.WebDriver;

public class SalaryObjects extends BasePage {
    WebDriver driver;
    public SalaryObjects(WebDriver driver) {
        this.driver = driver;
    }
}
